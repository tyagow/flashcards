import styled from 'styled-components/native'
import { viewBackground } from '../utils/colors'

export const ViewContainer = styled.View`
  background-color: ${viewBackground};
  flex: 1;
`

export default ViewContainer
